import { Breadcrumbs } from "./Breadcrumbs";
import { PageHero } from "@/components/layout/PageHero";
import { Section } from "@/components/ui/Section";
import { Button } from "@/components/ui/Button";
export function ArticleShell({title,summary,published,children,cta="/free-audit"}:{title:string;summary:string;published:string;children:React.ReactNode;cta?:string}){return <><Breadcrumbs items={[{name:"Home",path:"/"},{name:"Insights",path:"/insights"},{name:title,path:"#"}]}/><PageHero eyebrow="Regulus insight" title={title} lead={summary}/><Section><article className="prose-regulus mx-auto max-w-3xl"><p className="text-sm text-dim">By Regulus Automation Inc. · Published and updated {published}</p><div className="mt-10 space-y-8 text-ink-soft">{children}</div><div className="mt-12 rounded-3xl border border-line bg-panel p-8"><h2 className="text-h2 text-ink">Turn the question into an evidence-backed audit.</h2><p className="mt-4">The Free Time &amp; Workflow Recovery Audit separates observed friction from assumptions and defines a prioritized next step.</p><Button href={cta} className="mt-6" data-analytics-event="free_audit_cta">Review the free audit</Button></div></article></Section></>}
